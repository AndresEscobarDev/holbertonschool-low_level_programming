#include "holberton.h"
/**
 * _sqrt_recursion - function that returns the natural square root of a number.
 * @n: Number.
 * Return: n.
 */
int _sqrt_recursion(int n)
{
	if(n == 0)
	return (0);
	if (n < 0)
		return (-1);
	if (n > 0)
	{
		if (
		c = n - 1;
		_sqrt_recursion()

	}
}
