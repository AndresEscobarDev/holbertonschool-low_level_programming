#include "holberton.h"

void print_triangle(int size)
{
	int i, j;

	for (i = size; i > 0; i--)
	{
		for (j = 0; j < size; j++)
		{
			if (j < i - 1)
				_putchar(' ');
			else if (j >= 0)
				_putchar('#');
		}
		_putchar('\n');
	}
}
